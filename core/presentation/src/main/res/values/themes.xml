<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ZulipChat" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primaryDark</item>
        <item name="colorSecondary">@color/secondary</item>
        <item name="android:statusBarColor">@color/on_surface</item>
        <item name="android:textColor">@color/black_0</item>
        <item name="buttonStyle">@style/Theme.ZulipChat.Button</item>
    </style>

    <style name="Theme.ZulipChat.Button" parent="Widget.Material3.Button">
        <!-- Add any customizations here -->
        <item name="android:textAppearance">@style/TextAppearance.Material3.LabelLarge</item>
    </style>

    <style name="Theme.ZulipChat" parent="Base.Theme.ZulipChat" />

    <style name="Theme.ZulipChat.BottomSheet.Modal" parent="@style/Widget.Design.BottomSheet.Modal">
        <item name="android:background">@drawable/bg_bottom_sheet</item>
    </style>

    <style name="Theme.ZulipChat.BottomSheet" parent="@style/Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/Theme.ZulipChat.BottomSheet.Modal</item>
    </style>
</resources>